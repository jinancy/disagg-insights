package com.oracle.ugbu.ouai.ad.constants

object OCIConstants {
  /* Global Constants for Config Module*/
  // $COVERAGE-OFF$
  val OS_HOSTNAME = "params.os_hostname"
  val PEM_FILE_PATH = "params.private_key"
  val COMPARTMENT_ID = "params.compartment_id"
  val TENANCY_ID = "params.tenancy_id"
  val USER_ID = "params.user_id"
  val FINGERPRINT = "params.fingerprint"
  val NAMESPACE = "params.namespace"
  val PROXY_URL = "params.proxy_url"
  val INVENTORY_BUCKET = "params.inventory_bucket"
  val BUCKET_PREFIX = "params.bucket_prefix"
  val REGION = "params.region"
  val TENANT_INVENTORY_BUCKET = "params.tenant_inventory_bucket"
  // $COVERAGE-ON$
}
